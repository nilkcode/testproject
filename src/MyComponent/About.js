import React from "react";

export const About = () => {
  return (
    <div>
      <h4>Hello I am about function</h4>
      <p>
        A looks through its children and renders the first one that matches the
        current URL A looks through its children and renders the first one that
        matches the current URL A looks through its children and renders the
        first one that matches the current URL A looks through its children and
        renders the first one that matches the current URL A looks through its
        children and renders the first one that matches the current URL
      </p>
    </div>
  );
};
